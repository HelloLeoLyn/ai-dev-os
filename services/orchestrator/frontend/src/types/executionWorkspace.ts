export interface ExecutionWorkspace {
  id: string
  taskId: string
  projectId: string
  workspaceId: string
  sourceWorkspace: string
  executionWorkspace: string
  strategy: string
  status: string
  baseRevision: string
  createdAt: string
  updatedAt: string
}
