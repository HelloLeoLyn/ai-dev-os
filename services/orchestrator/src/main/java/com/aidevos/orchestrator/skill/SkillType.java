package com.aidevos.orchestrator.skill;

/**
 * Skill categories. Used to classify reusable agent skills and to map agents
 * to skills by capability when no explicit binding is configured.
 */
public enum SkillType {
	CODING,
	TESTING,
	BROWSER,
	ANALYSIS,
	DEPLOYMENT
}
